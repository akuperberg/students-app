import { Link } from "react-router-dom";

export const StudentCreateForm = () => {
    return (
        <div>
            <h1>Create student</h1>
            <nav><Link to="/">Back to student list</Link></nav>
        </div>
    );
};
